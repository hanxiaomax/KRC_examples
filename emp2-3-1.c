#include<stdio.h>
main()
{
	int i;
	printf("十进制\t八进制\t十六进制\t字符\n");	
	for(i=0;i<127;i++)
	printf("%d\t%#o\t%#x\t\t%c\n",i,i,i,i);/*打印整型数和相应的十六进制数以及字符（ASCII码）*/

	printf("%c\t%c\t%c\n",'41',41,'\41');
}

/*‘41’ ：单引号内只保存有一个字符，前面的会被后面的刷新掉，就是一个字符 ‘1’
41: 当整数作为字符型输出时，输出对应的ascii码，41对应的是);
‘\41’: 单引号内转义字符+数字，默认8进制,\x为16进制，8进制41=10进制33，ascii码33=!;*/
